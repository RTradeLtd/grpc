syntax = "proto3";

package models;

import "request/request.proto";
import "response/response.proto";

service Signer {
    rpc GetSignedMessage(request.SignRequest) returns (response.SignResponse) {}
}
